#include <bits/stdc++.h>
using namespace std;

int main(){
    
    int mini = 1e9;
    
    int n, k, l, c, d, p, nl, np;
    cin>>n>>k>>l>>c>>d>>p>>nl>>np;
    mini = min(mini, ((k*l)/(nl*n)));
    mini = min(mini, ((c*d)/(n)));
    mini = min(mini, ((p)/(np*n)));
    
    cout<<mini<<endl;
    
    return 0;
}
